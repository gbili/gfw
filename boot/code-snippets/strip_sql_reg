SELECT vc.slug AS catSlug, vc.value AS catName, i.localUrl AS imageLocalUrl, MAX(v.viewCount) FROM Vid_Category AS vc  RIGHT JOIN Vid AS v ON (vc.elementId = v.categoryId) RIGHT JOIN Vid_Image AS i ON (v.imageId = i.imageId) GROUP BY vc.elementId